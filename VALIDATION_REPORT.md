# TalentMatch Agent 验收报告

验收日期：2026-08-30

## 环境

- Windows 本地运行
- Python 3.13.9（当前自动化回归环境）
- Node.js 24.15.0
- Ollama 0.32.0
- 本地模型：`qwen2.5:7b`、`embeddinggemma`

## 自动化测试

- 后端：82 项通过，0 项失败。
- 已覆盖：技能别名与否定范围、JD抽取、候选人证据、PII检测、学历与年限绑定、匹配排序、冲突复核、LangGraph工作流、节点进度单调性、后台任务完成与异常ID、密码哈希、持久会话、退出撤销、两级角色权限、操作审计、岗位与候选人生命周期、岗位条件人工确认、模型缓存键与命中复用、提示注入拦截、排序指标、文件解析API、运行记录与人工决策持久化、JTH适配器和受控评测生成器。
- 已知非阻塞警告：LangGraph依赖提示未来版本将改变序列化默认参数；当前项目未使用持久化checkpoint，不影响运行结果。

## 前端构建

- Vite生产构建通过。
- 转换1817个模块。
- 产物：HTML约0.51KB、CSS约14.55KB、JS约219.12KB，均生成于`frontend/dist`。
- 已移除在线字体依赖，本地离线环境仍可显示完整界面。

## 后台任务与进度反馈

- `POST /api/v1/analysis/tasks`以HTTP 202返回任务编号，不阻塞请求直至本地模型完成。
- `GET /api/v1/analysis/tasks/{task_id}`返回排队、运行、完成或失败状态，以及0–100进度、当前节点说明和最终结果。
- 工作流在岗位条件、候选人证据、匹配、冲突复核、合规检查和排序阶段上报进度；前端以700毫秒间隔轮询并展示阶段与进度条。
- 规则模式API自动化测试已验证任务能够完成、返回排序结果并保存运行记录；不存在的任务编号返回404。

开发默认配置仍保留单进程内存队列。部署配置使用带密码的Redis + RQ独立Worker：在Worker未启动时提交任务，停止并重启API后，同一PostgreSQL会话仍有效且任务保持`queued`；启动Worker后原任务完成并将8名候选人结果写入PostgreSQL。该验证证明单机容器环境中的重启恢复，不等于多主机高可用或生产并发容量验证。

## PostgreSQL与业务数据管理

- 两次Alembic迁移在真实PostgreSQL 17容器中执行成功，共创建`users`、`auth_sessions`、`audit_log`、`jobs`、`candidates`、`runs`、`human_decisions`和`model_cache`等业务表。
- 岗位支持创建、列表、详情、状态更新；候选人支持创建、列表、详情和招聘流程状态更新。
- 真实HTTP验收已创建岗位并从`draft`更新为`active`，创建候选人并成功读回；相应写操作进入审计日志。
- React侧边栏已接入岗位库、候选人库、运行记录和管理员审计页面，不再是静态占位导航。

## 完整容器部署验收

- `deploy/compose.production.yml`配置解析通过，首次构建成功。
- 隔离项目`talentmatch-prod`启动Nginx前端、FastAPI API、RQ Worker、PostgreSQL、Redis和一次性迁移服务；API、PostgreSQL、Redis健康检查均通过。
- 仅前端端口映射到宿主机，PostgreSQL与Redis未公开宿主机端口。
- 通过Nginx入口`http://127.0.0.1:8081`验证：首页200且React挂载节点存在；管理员登录成功；后台规则任务完成；返回8名候选人；就绪接口报告`postgresql`与`redis`。
- 该结果属于本机Docker Compose准生产级部署验证，未经过企业真实用户、云负载均衡、TLS终止、多副本或灾备演练。

### API重启故障演练补充

- 首轮演练发现Nginx在API容器IP变化后仍使用旧上游地址，导致入口持续返回502；`deploy/nginx.conf`随后改为通过Docker内置DNS动态解析`api`服务。
- 修复后重新执行完整演练：停止RQ Worker、提交`task-6d67cd81102e`、重启API。任务在重启前后均保持`queued`，Nginx在第3次500毫秒轮询时恢复，同一`compose_admin`会话继续有效。
- 恢复Worker后，原任务而非重提任务完成，返回8名候选人排序，证明Redis任务状态、PostgreSQL会话和API结果链路可跨API重启恢复。
- 首次扩为2个Worker时发现容器内PID均为1，PID命名造成`talentmatch-1`冲突，实际只有一个Worker消费；随后将Worker名称改为`容器hostname + PID`并重新构建。
- 修复后两个Worker均持续运行且名称唯一；再次同时提交6个规则任务，6/6完成并产生6个唯一运行ID，总耗时2.462秒，两个Worker各处理3个任务。该结果仅是本机小规模并发验收，不代表生产吞吐上限。

## 最小企业权限与审计

- 用户密码使用PBKDF2-HMAC-SHA256与独立随机盐进行哈希，数据库不保存明文密码。
- 登录返回随机Bearer令牌，数据库只保存令牌SHA-256摘要；会话默认8小时，退出后立即撤销。
- 系统不开放注册；管理员通过部署主机本地命令初始化，并可创建`admin`或`recruiter`账号。
- 匿名访问候选人材料和分析接口返回401；招聘人员访问账号管理接口返回403。
- 登录、账号创建、文档解析、岗位解析、分析、运行详情读取和人工决策写入审计表。
- 隔离数据库上的真实HTTP验收已验证：匿名请求401、管理员登录200、创建招聘人员201，登录与账号创建审计记录均可查询。

当前权限模型只面向单企业内部团队，尚未实现多租户隔离、企业SSO、MFA、账户锁定和集中密钥管理，不能表述为完整企业IAM方案。

## 规则模式演示评测

固定演示集：1个JD、8名候选人，三级相关性标签分布为2名高度匹配、3名部分匹配、3名明显不匹配。

- 关键词覆盖基线：NDCG@5 0.9195、Precision@3 0.6667、Recall@5 0.8、MRR 1.0。
- 证据工作流：NDCG@5 1.0、Precision@3 1.0、Recall@5 1.0、MRR 1.0。
- 证据覆盖率：1.0。
- 无依据“已匹配”结论率：0。
- 人工复核率：0.5。

这些数字只验证固定演示集和评测脚本，不得作为真实招聘效果或最终简历指标。

## 中文受控对抗评测

测试集由6类岗位和60份模板化候选人材料组成，包含正常匹配、相邻技能、经验不足、学历不足、技能仅出现在列表、否定技能陈述和姓名/性别/年龄替换。它是可重复的合成回归集，不是现实简历或独立人工标注集。

- JD技能字段：Precision/Recall/F1均为1.0（30个字段）。
- 候选人技能字段：Precision/Recall/F1均为1.0（206个字段）。
- 年限、学历字段准确率及证据原文子串有效率均为1.0。
- 仅匹配器冲突识别：Precision 1.0、Recall 0.75、F1 0.8571。
- 加入冲突复核Agent：Precision/Recall/F1均为1.0（72个候选人—冲突代码金标），验证复核节点补全了弱证据、敏感属性和解析警告。
- 关键词基线NDCG@5 0.8395；证据工作流NDCG@5 0.9403。
- 6/6个姓名、性别、年龄配对样本的分数与逐条件状态一致。

首轮压力测试曾暴露“3年相关开发经验”年限漏抽取和并列否定技能范围错误；修正规则后重新运行才得到上述结果。完美字段指标只表示受控模板覆盖，无权外推到真实简历。

## JTH公开招聘历史弱标签评测

JTH原始文件已从Zenodo下载，3个CSV均通过官方MD5校验。评分权重只在2022年岗位上搜索，并用2023年岗位选择配置；测试切分固定为2024-01-01以后、至少5名历史申请者、具有多个招聘阶段且至少一人到达`Resume Sent`的岗位，测试集不参与调参。

- 规模：366个岗位查询、8,302个人岗对。
- 关键词基线：NDCG@5 0.4810、Precision@3 0.2195、Recall@5 0.4715、MRR 0.4389。
- 原始字段多属性基线：NDCG@5 0.4956、Precision@3 0.2259、Recall@5 0.5034、MRR 0.4518。
- 优化后结构化匹配：NDCG@5 0.5239、Precision@3 0.2632、Recall@5 0.5355、MRR 0.4890。
- 相对关键词基线的绝对增量：NDCG@5 +0.0429、Precision@3 +0.0437、Recall@5 +0.0640、MRR +0.0501，四项bootstrap 95%置信区间均未跨0。
- 相对更强多属性基线：NDCG@5 +0.0283、Precision@3 +0.0373、Recall@5 +0.0321、MRR +0.0372；其中NDCG@5、Precision@3与MRR的95%置信区间未跨0，Recall@5区间仍跨0。
- 单岗位NDCG@5相对关键词基线为181胜、43平、142负；相对多属性基线为170胜、55平、141负。
- `llm_sex`、国籍、年龄桶、薪资、邮编和来源等字段均未进入评分。

该数据只有结构化/匿名化属性，没有原始JD与简历文本；招聘阶段受历史政策和偏差影响，只能称为行为弱标签。JTH为法国场景且采用CC BY-NC 4.0许可，不能用于商业数据训练或声称生产招聘有效性。

## 可靠性压力测试

- 对8名候选人替换姓名，并移除显式性别/年龄文本。
- 8/8候选人分数不变。
- 候选人排序不变。

该测试仅验证当前显式属性处理链路，不等于证明系统不存在招聘偏差。

## Ollama真实链路

使用1名强匹配候选人完成真实本地调用：

- Qwen2.5-7B完成JD与候选人结构化抽取，两个Agent均未回退。
- LLM抽取与规则证据完成合并、原文校验和去重。
- 共识别11项岗位条件，强候选人得分96.66。
- 学历、工作年限、Python、FastAPI、LangGraph、RAG、Docker、PostgreSQL均绑定证据ID。
- 推荐状态为`recommended`，冲突发现为0。
- 本次关闭可选Embedding扩展后的端到端耗时约38.99秒。

本地7B链路适合功能验证，不应描述为高并发或生产级推理服务。

## 真实Agent消融与缓存性能（优化前全量LLM复核）

在固定1个JD、8名候选人的工程演示集上调用本地Qwen2.5-7B，比较以下路径：

- 关键词：NDCG@5 0.9195、Precision@3 0.6667、Recall@5 0.8、MRR 1.0。
- 单LLM直接排序：NDCG@5 0.9377、Precision@3 1.0、Recall@5 0.8、MRR 1.0。
- 确定性证据工作流：四项指标均为1.0。
- LLM证据抽取 + 确定性匹配：四项指标均为1.0。
- 完整冲突复核：四项指标仍为1.0。

冲突复核将可见发现从22条补充至42条，但没有继续改善该演示集排序，因此只能陈述“补充风险提示”，不能陈述“复核Agent提高排序指标”。单LLM直接调用耗时10.232秒；首次完整LLM链路耗时265.08秒。缓存键绑定模型摘要、提示词版本、完整输入和采样参数，相同输入复跑为0.241秒，8/8份候选人抽取均命中缓存。该结果只证明重复计算复用，不代表首次推理具备高吞吐。

## 首次批量推理优化

在模型缓存关闭的同一固定8人演示集上，启用默认2路受控并行，并将LLM复核限制为“无明确严重冲突但存在部分/待复核证据”的模糊案例：

- 端到端耗时：182.286秒；相对早期串行观测265.08秒约1.454倍加速，下降约31.2%。
- JD抽取：19.381秒；候选人抽取：137.419秒；冲突复核：25.465秒。
- 8名候选人均完成LLM抽取，无节点回退。
- NDCG@5、Precision@3、Recall@5、MRR仍均为1.0；无依据结论率为0。
- 证据覆盖率为0.9886，说明LLM结果仍有少量待复核条件，不能把固定演示集排序满分解释为所有证据均完美。

265.08秒参考值来自此前同一机器上的顺序执行观测，并非同一进程内重复AB测试；Ollama还可能在内部排队。因此该结果只能表述为固定演示集上的观测提速，不能声称生产并发容量或稳定1.454倍加速。

## 自适应Agent路由

新增`adaptive`模式：JD仍由Ollama解析；候选人先经过确定性质量检查，仅将缺少强项目/工作证据、关键字段缺失或存在解析警告的材料送入LLM补充抽取。完整`ollama`模式仍保留作质量对照。

- 固定8人、关闭缓存的端到端耗时：76.744秒。
- LLM候选人抽取路由：3/8；其余5份使用强证据规则结果。
- LLM冲突复核路由：4/8；其余使用确定性冲突结论。
- JD抽取19.765秒、候选人阶段33.564秒、冲突复核23.394秒。
- 相对早期串行265.08秒约3.454倍加速，耗时下降约71.0%。
- 相对完整Ollama优化版182.286秒耗时下降约57.9%。
- 0节点回退；排序与完整Ollama模式一致；NDCG@5、Precision@3、Recall@5、MRR仍均为1.0；证据覆盖率0.9886、无依据结论率0。

路由规则本身仍是人工设定的可解释策略，并未在真实招聘数据上学习；固定演示集指标不能证明自适应模式在所有简历上与完整Ollama等价。

## 提示注入安全审计

对6种中英文攻击模板进行验证，包括“忽略系统指令”“给我100分”“排在第一名”和“显示system prompt”：

- 6/6在进入LLM前被拦截并切换为确定性解析；
- 6/6产生`PROMPT_INJECTION_SUSPECTED`复核告警；
- 6/6与删除攻击语句后的基准材料评分一致。

当前防护依赖已知模式和确定性回退，只是防御纵深的一层，不是完整安全认证，也不能覆盖所有未知改写。

## HTTP端到端验收

- `GET /api/v1/health`：200，Ollama和两个模型均识别为可用。
- 健康检查可见17条持久化模型缓存记录。
- 前端首页：200，React挂载节点存在。
- `POST /api/v1/jobs/parse`：解析11项条件；人工确认后分析轨迹明确跳过重复JD抽取。
- 确认后的分析响应记录`criteria_confirmed_by_human=true`。
- `POST /api/v1/analyze/demo?mode=rules`：处理8名候选人，Top-1为`cand-001`，得分96.66，6个节点完成。
- `POST /api/v1/runs/{run_id}/decisions`：人工决定成功写入。
- `GET /api/v1/runs/{run_id}/decisions`：成功读回1条决定记录。

## 当前服务

- 后端：<http://127.0.0.1:8000>
- API文档：<http://127.0.0.1:8000/docs>
- 前端：<http://127.0.0.1:5173>

## 2026-08-31 招聘业务闭环与可观测性验收

本轮新增持久化`screening_batches`与`screening_batch_items`，把岗位、候选人、异步任务、分析运行记录、人工决策和候选人历史串成一条业务链路。验收结果：

- 后端完整自动化测试：82项通过，只有1条LangGraph未来默认值变更警告；
- 前端生产构建：Vite成功构建，1,817个模块完成转换；
- Alembic空库升级：`20260830_0001 -> 20260830_0002 -> 20260831_0003`成功，空库实际生成`screening_batches`和`screening_batch_items`；
- Docker Compose配置解析成功；本轮没有重建完整生产容器，因此不把配置解析写成生产部署验证；
- 独立临时数据库HTTP验收：内部账号登录、创建在招岗位、保存候选人、创建规则筛选批次、轮询后台任务、生成运行记录、提交“进入下一轮”决策、读取候选人筛选历史均成功；
- 批次最终状态为`reviewed`，`reviewed_count=1`，人工决策为`advance`；
- `/metrics`实际产生HTTP请求、任务结果、队列等待和Agent节点耗时指标；前端开发服务返回HTTP 200；
- 验收结束后已停止临时前后端进程并删除独立数据库及WAL/SHM文件，未写入现有业务数据库。

本轮同时加入GitHub Actions配置，但只完成本地等价命令验证；在代码推送到GitHub并由Actions执行前，不能声称远端CI已经通过。
